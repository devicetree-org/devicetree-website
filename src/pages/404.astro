---
import { getEntry } from "astro:content";
import BaseHead from "../components/head/BaseHead.astro";
import BaseLayout from "../layouts/BaseLayout.astro";

const about = await getEntry("pages", "404");
const { data, render } = about;
const { Content } = await render();
---

<html lang="en">
  <BaseHead title={"404 - DeviceTree"} description={data.description} />
  <BaseLayout>
    <section class="my-16 max-w-3xl mx-auto px-5">
      <h1>Page Not Found</h1>
      <p>
        We’re sorry but the page you are trying to access cannot be found.
      </p>
    </section>
  </BaseLayout>
</html>
